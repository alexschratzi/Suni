// Dummy-Screen für Menü-Tab (wird nie angezeigt, da wir es abfangen)
import { View, Text } from "react-native";

export default function MenuScreen() {
  return (
    <View>
      <Text>Menü (sollte nie sichtbar sein)</Text>
    </View>
  );
}
